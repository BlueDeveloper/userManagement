<?xml version="1.0" encoding="UTF-8"?>
<Configuration>

    <!-- 해당 설정파일에서 사용하는 프로퍼티-->
    <Properties>
        <property name="LOGS_PATH">C:\\portfolio\\userManagement\\project\\log\\</property>
        <property name="LOG_FILE_NAME">log</property>
    </Properties>

    <Appenders>
        <!-- 콘솔 -->
        <Console name="console" target="SYSTEM_OUT">
            <PatternLayout pattern="[%d{yyyy-MM-dd HH:mm:ss}:%-3relative] %-5level %logger{35} - %msg%n"/>
        </Console>

        <!-- 파일 -->
        <RollingFile name="RollingFile">
            <FileName>${LOGS_PATH}/${LOG_FILE_NAME}.log</FileName>
            <FilePattern>${LOGS_PATH}/${date:yyyy}/${date:MM}/${LOG_FILE_NAME}_%d{yyyy-mm-dd}.log</FilePattern>
            <PatternLayout>
                <Pattern>%d{yyyy-MM-dd HH:mm:ss} %5p [%c] %m%n</Pattern>
            </PatternLayout>
            <Policies>
                <SizeBasedTriggeringPolicy size="100MB"/>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <DefaultRolloverStrategy max="7" fileIndex="max">
                <Delete basePath="${LOGS_PATH}" maxDepth="1">
                    <!-- 7일이 경과한 로그파일은 자동 삭제 -->
                    <IfLastModified age="7d"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
        -
    </Appenders>

    <Loggers>
        <!-- Application Loggers -->
        <!-- Root 보다 우선 순위가 높음. -->
        <logger name="user.mngm.usermanagement.*" level="INFO" additivity="false">
            <AppenderRef ref="console"/>
            <AppenderRef ref="RollingFile"/>
        </logger>


        <!-- FATAL, ERROR, WARN, INFO, DEBUG, TRACE -->
        <Root level="INFO">
            <AppenderRef ref="console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>
    </Loggers>
</Configuration>
